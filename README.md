# Setting up

> composer install
> cp .env.example .env
> npm install
> set the database configuration on .env file
> php artisan key:generate
> php artisan migrate --seed
> npm run dev
> php artisan serve
